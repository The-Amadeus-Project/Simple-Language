# syntax

```cpp
int name 123; // no expression in val but func returns are goo
// int name eval("123 - 843");

out(name); // prints name

// no -= += \= *=
minus = eval(f"{name} + 123");

out(minus);
```